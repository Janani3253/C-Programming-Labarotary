#include<stdio.h>
void factors()
{
    int num;
    scanf("%d",&num);
   int i,c=0;
   for(i=1;i<=num;i++)
   {
       if(num%i==0)
       {
           c++;
       }
   }
   printf("%d",c);
}
int main()
{
    factors();
    return 0;
}
