#include <stdio.h>

int main()
{
   int a;
   float b;
   double c;
   char d;
   printf("size of integer is %ld\n",sizeof(a));
   printf("size of float is %ld\n",sizeof(b));
   printf("size of double is %ld\n",sizeof(c));
   printf("size of character is %ld",sizeof(d));
}
